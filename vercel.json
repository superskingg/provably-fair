{
  "builds": [
    {
      "src": "api/**/*.php",
      "use": "vercel-php@0.7.2",
      "config": {
        "phpIni": "api/php.ini"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.php" }
  ]
}
